services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq-container
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  extractor:
    build:
      context: .
      dockerfile: ./extractor/Dockerfile
    container_name: extractor-container
    ports:
      - "8000:8000"
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB
      - DB_HOST
      - DB_PORT
      - CELERY_BROKER_URL=${RABBITMQ_URL}
      - SUMMARY_QUEUE_NAME
    volumes:
      - epub_uploads:/app/uploads
      - epub_output:/app/output
    depends_on:
      extractor-init:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy

  worker:
    build:
      context: .
      dockerfile: ./worker/Dockerfile
    container_name: worker-container
    command:
      [
        "celery",
        "-A",
        "src.infrastructure.queue.app:app",
        "worker",
        "--loglevel=info",
        "-Q",
        "${SUMMARY_QUEUE_NAME:-summary_jobs}",
      ]
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB
      - LLM_MODEL
      - LLM_HOST
      - DB_HOST
      - DB_PORT
      - CELERY_BROKER_URL=${RABBITMQ_URL}
      - SUMMARY_QUEUE_NAME
    volumes:
      - epub_uploads:/app/uploads
      - epub_output:/app/output
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      ollama:
        condition: service_healthy
      
  extractor-init:
    build:
      context: .
      dockerfile: ./extractor/Dockerfile
    command: bash scripts/prestart.sh
    env_file:
      - .env
    container_name: extractor-init-container
    volumes:
      - epub_uploads:/app/uploads
      - epub_output:/app/output
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    container_name: frontend-container
    ports:
      - "3000:3000"
    environment:
      - NEXT_PRIVATE_API_URL
      - NEXT_PUBLIC_API_URL
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB
      - DB_HOST
      - DB_PORT
      - AUTH_SECRET

    depends_on:
      - db

  ollama:
    image: ollama/ollama:latest
    container_name: ollama-container
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    # restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 10

  # One-shot container: pulls required models and exits.
  # Re-runs are instant because models are cached in ollama_data volume.
  ollama-init:
    container_name: ollama-init-container
    image: ollama/ollama:latest
    depends_on:
      ollama:
        condition: service_healthy
    volumes:
      - ollama_data:/root/.ollama
    environment:
      - OLLAMA_HOST=${LLM_HOST}
    entrypoint: ["/bin/sh", "-c", "ollama pull ${LLM_MODEL} && ollama pull ${EMBEDDING_MODEL}"]
    restart: "no"


  
  db:
    image: postgres
    container_name: db-container
    restart: always
    # set shared memory limit when using docker compose
    shm_size: 128mb
    # or set shared memory limit when deploy via swarm stack
    #volumes:
    #  - type: tmpfs
    #    target: /dev/shm
    #    tmpfs:
    #      size: 134217728 # 128*2^20 bytes = 128Mb
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  pgadmin4:
    image: dpage/pgadmin4
    ports:
      - 80:80
    container_name: pgadmin4-container
    volumes:
      # https://www.pgadmin.org/docs/pgadmin4/latest/container_deployment.html
      - pgadmin_storage:/var/lib/pgadmin
    environment:
      - PGADMIN_DEFAULT_EMAIL
      - PGADMIN_DEFAULT_PASSWORD


volumes:
  pgadmin_storage:
  ollama_data:
  epub_uploads:
  epub_output:
