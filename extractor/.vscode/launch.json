{
    "version": "0.2.0",
    "configurations": [

                {
            "name": "try",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/mediator.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "python": "${command:python.interpreterPath}",
        }

    ]
}